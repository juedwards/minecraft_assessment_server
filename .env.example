# .env.example - copy to .env and fill values
# Choose whether to use Azure DefaultAzureCredential for authentication. If true, the app will try
# to authenticate using DefaultAzureCredential (Managed Identity, Azure CLI, environment-based SP, etc.)
# If false (default), the app will use the AZURE_OPENAI_API_KEY if provided.
AZURE_USE_DEFAULT_CREDENTIALS=false

# Azure OpenAI (optional — required for AI analysis when not using DefaultAzureCredential)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://ai-mcedudev987518176632.openai.azure.com/openai/deployments/gpt-4o/chat/completions?api-version=2025-01-01-preview
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Server ports (optional)
MINECRAFT_PORT=19131
HTTP_PORT=8080
WS_PORT=8081

# Notes:
# - If you want to use your Azure login or a managed identity, set AZURE_USE_DEFAULT_CREDENTIALS=true
#   and ensure the environment or VM has a valid credential (e.g. `az login` locally, or an assigned MI).
# - If you do not want AI features, you can leave the Azure variables empty.
# - After copying to .env, edit values and keep this file private (do not check into source control).